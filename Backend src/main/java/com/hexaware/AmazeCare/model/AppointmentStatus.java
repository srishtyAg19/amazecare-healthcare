package com.hexaware.AmazeCare.model;

public enum AppointmentStatus {
	SCHEDULED,  // Initial state when an appointment is created
    CONFIRMED,  // When the doctor accepts the appointment
    COMPLETED,  // After the consultation is done
    CANCELLED 

}
